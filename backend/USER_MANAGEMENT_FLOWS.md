# User Management Flow Diagrams

## 1. Create User Flow (Tenant-Isolated)

```
┌─────────────────────────────────────────────────────────────┐
│ Client Request                                              │
│ POST /api/users                                             │
│ Authorization: Bearer <jwt-token>                           │
│                                                             │
│ Body:                                                       │
│ {                                                           │
│   "email": "newuser@acme.com",                              │
│   "password": "password123",                                │
│   "firstName": "New",                                       │
│   "lastName": "User",                                       │
│   "role": "ORG_MEMBER"                                      │
│   // NO organizationId in body!                            │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ authenticate middleware                                     │
│ - Extract JWT from Authorization header                     │
│ - Verify token signature                                    │
│ - Decode payload                                            │
│                                                             │
│ Sets:                                                       │
│ req.user = {                                                │
│   userId: "creator-uuid",                                   │
│   email: "owner@acme.com",                                  │
│   role: "ORG_OWNER",                                        │
│   organizationId: "org-uuid"                                │
│ }                                                           │
│ req.tenantId = "org-uuid"                                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ requireOrganization middleware                              │
│ - Check req.user.organizationId exists                      │
│ - Reject if SUPER_ADMIN (no organization)                   │
│                                                             │
│ ✅ Pass: User has organization                              │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ tenantContext middleware                                    │
│ - Validate organization exists in database                  │
│ - Check organization status is "active"                     │
│ - Confirm req.tenantId is set                               │
│                                                             │
│ ✅ Pass: Organization is valid and active                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ authorize('ORG_OWNER', 'ORG_ADMIN') middleware              │
│ - Check req.user.role                                       │
│ - Verify role is in allowed list                            │
│                                                             │
│ ✅ Pass: User is ORG_OWNER or ORG_ADMIN                     │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.controller.createUser                                  │
│                                                             │
│ 1. Validate request body fields                             │
│ 2. Validate email format                                    │
│ 3. Validate password strength                               │
│                                                             │
│ 4. CRITICAL: Get organizationId from token                  │
│    const organizationId = req.tenantId;  ← From JWT!        │
│                                                             │
│ 5. SECURITY: Reject if organizationId in body               │
│    if (req.body.organizationId) {                           │
│      throw Error('organizationId cannot be specified');     │
│    }                                                        │
│                                                             │
│ 6. Call service                                             │
│    await userService.createUser(data, organizationId);      │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.service.createUser                                     │
│                                                             │
│ 1. Validate organizationId is provided                      │
│ 2. Validate role is organization role (not SUPER_ADMIN)     │
│ 3. Check email uniqueness in organization                   │
│ 4. Hash password with bcrypt                                │
│ 5. Call repository with organizationId from token           │
│                                                             │
│ await userRepository.createUser(data, organizationId);      │
│                                    ↑                        │
│                          From token, NOT request!           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.repository.createUser                                  │
│                                                             │
│ return await prisma.user.create({                           │
│   data: {                                                   │
│     email,                                                  │
│     passwordHash,                                           │
│     firstName,                                              │
│     lastName,                                               │
│     role,                                                   │
│     organizationId,  ← ENFORCED from token                  │
│   }                                                         │
│ });                                                         │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Database (PostgreSQL)                                       │
│                                                             │
│ INSERT INTO users (                                         │
│   email, password_hash, first_name, last_name,              │
│   role, organization_id                                     │
│ ) VALUES (                                                  │
│   'newuser@acme.com', '$2a$10$...', 'New', 'User',          │
│   'ORG_MEMBER', 'org-uuid'  ← From token!                   │
│ );                                                          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Response (201 Created)                                      │
│ {                                                           │
│   "success": true,                                          │
│   "message": "User created successfully",                   │
│   "data": {                                                 │
│     "id": "new-user-uuid",                                  │
│     "email": "newuser@acme.com",                            │
│     "firstName": "New",                                     │
│     "lastName": "User",                                     │
│     "role": "ORG_MEMBER",                                   │
│     "organizationId": "org-uuid",  ← Same as creator's org  │
│     "organization": {                                       │
│       "id": "org-uuid",                                     │
│       "name": "Acme Corp",                                  │
│       "slug": "acme-corp"                                   │
│     },                                                      │
│     "isActive": true,                                       │
│     "createdAt": "2026-01-28T00:00:00.000Z"                 │
│   }                                                         │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. List Users Flow (Tenant-Scoped)

```
┌─────────────────────────────────────────────────────────────┐
│ Client Request                                              │
│ GET /api/users?page=1&limit=10                              │
│ Authorization: Bearer <jwt-token>                           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ authenticate middleware                                     │
│ req.user = { userId, email, role, organizationId }          │
│ req.tenantId = "org-uuid"                                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.controller.getUsers                                    │
│                                                             │
│ Check user role:                                            │
└─────────────────────────────────────────────────────────────┘
                          ↓
                    ┌─────┴─────┐
                    │           │
         ┌──────────▼──┐    ┌───▼──────────┐
         │ SUPER_ADMIN │    │   ORG_USER   │
         └──────────┬──┘    └───┬──────────┘
                    │           │
                    │           │ const organizationId = req.tenantId;
                    │           │ (From token)
                    │           │
         Can filter │           │ Automatically scoped
         by orgId   │           │ to their organization
                    │           │
                    └─────┬─────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.service.getUsersByOrganization                         │
│                                                             │
│ await userRepository.findByOrganization(organizationId);    │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.repository.findByOrganization                          │
│                                                             │
│ const where = {                                             │
│   organizationId,  ← CRITICAL: Tenant filter                │
│   ...otherFilters                                           │
│ };                                                          │
│                                                             │
│ return await prisma.user.findMany({ where });               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Database Query                                              │
│                                                             │
│ SELECT * FROM users                                         │
│ WHERE organization_id = 'org-uuid'  ← From token!           │
│ ORDER BY created_at DESC                                    │
│ LIMIT 10 OFFSET 0;                                          │
│                                                             │
│ Returns: ONLY users from requester's organization           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Response (200 OK)                                           │
│ {                                                           │
│   "success": true,                                          │
│   "message": "Users retrieved successfully",                │
│   "data": [                                                 │
│     {                                                       │
│       "id": "user-1",                                       │
│       "email": "owner@acme.com",                            │
│       "role": "ORG_OWNER",                                  │
│       "organizationId": "org-uuid"  ← Same org              │
│     },                                                      │
│     {                                                       │
│       "id": "user-2",                                       │
│       "email": "member@acme.com",                           │
│       "role": "ORG_MEMBER",                                 │
│       "organizationId": "org-uuid"  ← Same org              │
│     }                                                       │
│   ],                                                        │
│   "pagination": { "page": 1, "limit": 10, "total": 2 }     │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. Security: organizationId Rejection Flow

```
┌─────────────────────────────────────────────────────────────┐
│ ❌ MALICIOUS Request                                        │
│ POST /api/users                                             │
│ Authorization: Bearer <jwt-token>                           │
│                                                             │
│ Body:                                                       │
│ {                                                           │
│   "email": "hacker@evil.com",                               │
│   "password": "password123",                                │
│   "firstName": "Evil",                                      │
│   "lastName": "Hacker",                                     │
│   "organizationId": "different-org-id"  ← ATTACK!           │
│ }                                                           │
│                                                             │
│ Attacker trying to create user in different organization!   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ authenticate middleware                                     │
│ req.user = {                                                │
│   organizationId: "attacker-org-uuid"                       │
│ }                                                           │
│ req.tenantId = "attacker-org-uuid"                          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.controller.createUser                                  │
│                                                             │
│ // SECURITY CHECK                                           │
│ if (req.body.organizationId) {                              │
│   throw new Error(                                          │
│     'organizationId cannot be specified in request body'    │
│   );                                                        │
│ }                                                           │
│                                                             │
│ ❌ REQUEST REJECTED!                                        │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Error Response (400 Bad Request)                            │
│ {                                                           │
│   "success": false,                                         │
│   "message": "organizationId cannot be specified in         │
│                request body"                                │
│ }                                                           │
│                                                             │
│ ✅ ATTACK PREVENTED!                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. Cross-Tenant Access Prevention

```
┌─────────────────────────────────────────────────────────────┐
│ Scenario: User from Org A tries to access user from Org B   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Client Request (User from Org A)                            │
│ GET /api/users/user-from-org-b-uuid                         │
│ Authorization: Bearer <jwt-token-org-a>                     │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ authenticate middleware                                     │
│ req.user = {                                                │
│   userId: "user-a-uuid",                                    │
│   organizationId: "org-a-uuid"  ← User is from Org A        │
│ }                                                           │
│ req.tenantId = "org-a-uuid"                                 │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.controller.getUserById                                 │
│ const organizationId = req.tenantId;  // "org-a-uuid"       │
│ await userService.getUserById(userId, organizationId);      │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.repository.findByIdInOrganization                      │
│                                                             │
│ return await prisma.user.findFirst({                        │
│   where: {                                                  │
│     id: "user-from-org-b-uuid",                             │
│     organizationId: "org-a-uuid"  ← Tenant filter           │
│   }                                                         │
│ });                                                         │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Database Query                                              │
│                                                             │
│ SELECT * FROM users                                         │
│ WHERE id = 'user-from-org-b-uuid'                           │
│   AND organization_id = 'org-a-uuid';  ← No match!          │
│                                                             │
│ Result: NULL (user not found)                               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Error Response (404 Not Found)                              │
│ {                                                           │
│   "success": false,                                         │
│   "message": "User not found in your organization"          │
│ }                                                           │
│                                                             │
│ ✅ CROSS-TENANT ACCESS PREVENTED!                           │
│ User from Org B remains hidden from Org A                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. Update User Flow (Tenant-Scoped)

```
┌─────────────────────────────────────────────────────────────┐
│ Client Request                                              │
│ PATCH /api/users/user-uuid                                  │
│ Authorization: Bearer <jwt-token>                           │
│                                                             │
│ Body:                                                       │
│ {                                                           │
│   "role": "ORG_ADMIN",                                      │
│   "isActive": true                                          │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Middleware Chain                                            │
│ authenticate → requireOrganization → tenantContext →        │
│ authorize('ORG_OWNER', 'ORG_ADMIN')                         │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.controller.updateUser                                  │
│                                                             │
│ const organizationId = req.tenantId;  // From token         │
│                                                             │
│ // SECURITY: Reject organizationId modification             │
│ if (req.body.organizationId) {                              │
│   throw Error('organizationId cannot be modified');         │
│ }                                                           │
│                                                             │
│ await userService.updateUser(userId, organizationId, data); │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.service.updateUser                                     │
│                                                             │
│ // Check user exists in organization                        │
│ await this.getUserById(userId, organizationId);             │
│                                                             │
│ // Remove organizationId from data (security)               │
│ delete data.organizationId;                                 │
│                                                             │
│ await userRepository.updateUserInOrganization(...);         │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ user.repository.updateUserInOrganization                    │
│                                                             │
│ return await prisma.user.updateMany({                       │
│   where: {                                                  │
│     id: userId,                                             │
│     organizationId,  ← CRITICAL: Tenant filter              │
│   },                                                        │
│   data                                                      │
│ });                                                         │
│                                                             │
│ Using updateMany (not update) for safety:                   │
│ - If user not in org, count = 0 (no update)                 │
│ - Prevents cross-tenant updates                             │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Database Query                                              │
│                                                             │
│ UPDATE users                                                │
│ SET role = 'ORG_ADMIN', is_active = true                    │
│ WHERE id = 'user-uuid'                                      │
│   AND organization_id = 'org-uuid';  ← Tenant filter        │
│                                                             │
│ ✅ Only updates if user is in requester's organization      │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. SUPER_ADMIN vs ORG_USER Comparison

### SUPER_ADMIN Flow
```
┌─────────────────────────────────────────────────────────────┐
│ SUPER_ADMIN Request                                         │
│ POST /api/users                                             │
│ Authorization: Bearer <super-admin-token>                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ authenticate middleware                                     │
│ req.user = {                                                │
│   role: "SUPER_ADMIN",                                      │
│   organizationId: null  ← No organization                   │
│ }                                                           │
│ req.tenantId = null                                         │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ requireOrganization middleware                              │
│                                                             │
│ if (!req.user.organizationId) {                             │
│   return 403 Forbidden                                      │
│ }                                                           │
│                                                             │
│ ❌ REJECTED: SUPER_ADMIN has no organization                │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Error Response (403 Forbidden)                              │
│ {                                                           │
│   "success": false,                                         │
│   "message": "This endpoint requires organization           │
│                membership"                                  │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
```

### ORG_USER Flow
```
┌─────────────────────────────────────────────────────────────┐
│ ORG_OWNER Request                                           │
│ POST /api/users                                             │
│ Authorization: Bearer <org-owner-token>                     │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ authenticate middleware                                     │
│ req.user = {                                                │
│   role: "ORG_OWNER",                                        │
│   organizationId: "org-uuid"  ← Has organization            │
│ }                                                           │
│ req.tenantId = "org-uuid"                                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ requireOrganization middleware                              │
│ ✅ PASS: User has organization                              │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ tenantContext middleware                                    │
│ ✅ PASS: Organization is active                             │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ authorize('ORG_OWNER', 'ORG_ADMIN')                         │
│ ✅ PASS: User is ORG_OWNER                                  │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Controller → Service → Repository                           │
│ ✅ User created in requester's organization                 │
└─────────────────────────────────────────────────────────────┘
```

---

## Summary

### Key Security Principles

1. **organizationId Source**: ALWAYS from `req.tenantId` (JWT token)
2. **Request Body Protection**: `organizationId` in body is REJECTED
3. **Repository Filtering**: ALL queries include `organizationId` filter
4. **Cross-Tenant Prevention**: Users cannot access data from other organizations
5. **Role-Based Access**: Only authorized roles can perform actions

### Tenant Isolation Layers

```
Layer 1: JWT Token
  ↓ organizationId embedded in token
Layer 2: Middleware
  ↓ Extracts and validates organizationId
Layer 3: Controller
  ↓ Rejects organizationId from request body
Layer 4: Service
  ↓ Enforces tenant isolation rules
Layer 5: Repository
  ↓ Filters ALL queries by organizationId
Layer 6: Database
  ↓ Only returns data from user's organization
```

**Multi-tenant security is enforced at EVERY layer!** 🔒
